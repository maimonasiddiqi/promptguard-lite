[build-system]
requires=["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "promptguard-lite"
version = "0.1.0"
dependencies=[]

[project.scripts]
promptguard = "cli:main"

[tool.setuptools]
packages = ["detectors"]
py-modules = ["cli", "scanner", "models", "base", "utils", "exceptions", "demo"]
